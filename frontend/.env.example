# URL of the deployed Django backend (e.g. https://spotter-trip-planner.onrender.com).
# Leave empty in development to use the Vite proxy (which forwards /api to http://127.0.0.1:8001).
VITE_API_URL=
